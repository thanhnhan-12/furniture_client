import React from 'react';
import { Box, Typography } from '@mui/material';

const Customers = () => {
  return (
    <Box>
      <Typography>Cutomers</Typography>
    </Box>
  );
};

export default Customers;
